---
type: summary
agent: 心法规则官
task: 把 ../real-agent 的 6 条纯心法层 global 规则忠实继承、剥离前端招式、诚实适配到 api2mcp4j（Java 库）
date: 2026-06-24 11:15
status: completed
related: docs/rules/global/*.md, docs/rules/README.md, CLAUDE.md
tags: [规则继承, 心法层, 破坏性删除治理, Java适配]
---

# 工作总结 · global 规则继承

## 一、任务

董事长 2026-06-24 立宪级指示「全面继承 ../real-agent 开发心法」。本次承接其中"纯心法层 global 规则"子任务：把 real-agent `docs/rules/global/` 的 6 条规则 + README，剥离前端/设计招式，诚实适配到本项目（api2mcp4j / 内部名 server2mcp，一个把 Spring `@RestController` 自动暴露为 MCP Tool/Resource/Prompt/Complete 的纯 Java 库）的 Java/Maven/Spring/MCP 语境，落地为本项目 `docs/rules/global/` 6 文件 + `docs/rules/README.md`。

## 二、过程（关键决策点）

1. **先宣誓后开工**：输出 Briefback 六项（含第六项环境能力自检），未宣誓不动手。
2. **实测工具表，拒绝凭空捏造**（核心诚实动作）：
   - `which sg ast-grep` → **未安装**，结构层无 AST 工具
   - `which jdtls` → 二进制在 `/opt/homebrew/bin/jdtls`，但 ToolSearch 查无任何 LSP MCP 桥接工具 → 语义层"二进制存在 ≠ 会话可调用"，分开诚实声明
   - `java -version` / `mvn -version` → JDK 17 / mvn 3.9.12，与项目要求一致
3. **摸清本项目真实公开 API 表面**（破坏性删除规则的落点）：注解（@McpTool 及 converter 等属性）、Provider 公开类、解析器 SPI（AbstractDesParser/AbstractParamParser）、IXxxContext 接口、回调抽象类、McpCallToolResultConverter。
4. **纠正一处 CLAUDE.md 与代码的出入**：CLAUDE.md 称解析器"按 order 0-5 排列"，实测 `@Order(0..5)` + `@ConditionalOnParser(value=..., type=...)` 是标注在 **server2mcp-autoconfigure/McpConfig.java** 的 @Bean 工厂方法上（不在 core 解析器类上）。规则示例据真实代码编写，未沿用 CLAUDE.md 的近似表述。
5. **图谱维度诚实落地**：real-agent 的 `impact_radius` 适配为"多模块 Maven 反向依赖分析 + 下游语义化版本兼容性"——因本项目是 common←core←autoconfigure←starters 多模块库且产物被下游依赖。
6. **README 据实改写**：明确**不引入** task.json 意图协议（单一 Java 库无多模块意图锁，属过度设计），不照搬 15 modules/12 layers 全栈目录；global 默认对所有会话生效。

## 三、结果（产出物清单 · 绝对路径）

| 文件 | 字节 | 说明 |
|------|------|------|
| /Users/ls/code/api2mcp4j/docs/rules/global/search-tool-parity.md | 7936 | 搜索四维；结构层诚实降级为 grep+人工代偿 |
| /Users/ls/code/api2mcp4j/docs/rules/global/destructive-deletion.md | 11363 | 重点文件；落到注解/Provider/SPI/Context + 多模块下游兼容性 |
| /Users/ls/code/api2mcp4j/docs/rules/global/session-continuity.md | 3509 | 续接先验 git；补 server2mcp-test 不在根 modules 陷阱 |
| /Users/ls/code/api2mcp4j/docs/rules/global/refactor-ordering.md | 4814 | 契约提供者先行；按模块依赖流向 + SNAPSHOT 发布语境 |
| /Users/ls/code/api2mcp4j/docs/rules/global/agent-capability-declaration.md | 5763 | 物理不可能性 + Briefback 第六项；能力清单按实测填 |
| /Users/ls/code/api2mcp4j/docs/rules/global/work-log.md | 2937 | 工作留痕；路径适配 docs/logs/ |
| /Users/ls/code/api2mcp4j/docs/rules/README.md | 4198 | 据实改写；不引入 task.json |
| /Users/ls/code/api2mcp4j/docs/logs/2026-06-24_心法规则官_global规则继承.md | 本文件 | 工作总结 |

## 四、发现（意外 / 风险）

1. **CLAUDE.md 的"order 0-5"表述与代码实现位置有出入**（见过程 4）。规则文件已据真实代码（McpConfig.java 的 @Bean + @Order + @ConditionalOnParser）编写。**建议 CEO 知悉**：CLAUDE.md「双层解析器链」段落把 @Order 归在 core 解析器类，实际在 autoconfigure——若 CEO 后续操刀 CLAUDE.md，可顺手校正。
2. **语义层（LSP）可用性是会话级变量**：jdtls 二进制装了，但本会话工具表未暴露 LSP MCP 桥接。规则已诚实写为"桥接未激活时降级"，而非断言可用或不可用——这是最忠实的表述。
3. **结构层 AST 工具本环境确实没有**：规则全程用"若具备 AST 工具则用之；否则 grep 多形式 + 人工结构审查代偿"措辞，未捏造任何工具名。
4. `agent-capability-declaration.md` 第 63 行**出现了 real-agent 的 mcp__ 工具名**——这是**禁令引用**（"严禁照抄……本环境不存在这些工具"），是诚实警示而非捏造使用，已核实语境，保留。

## 五、建议（后续 / 需 CEO 复核点）

1. **请 CEO/御史台复核诚实适配三处**：(a) 图谱层=多模块 Maven 反向依赖 + 下游兼容性，是否认可这个 impact_radius 的等价替代；(b) LSP"桥接未激活时降级"的表述是否够清晰；(c) 结构层全程"代偿"措辞是否被接受为合规，而非视作能力缺失导致规则空转。
2. **CLAUDE.md 解析器 @Order 位置表述**（发现 1）建议 CEO 操刀 CLAUDE.md 时一并校正——本次未动 CLAUDE.md（红线）。
3. 若后续会话暴露了 LSP MCP 桥接或安装了 ast-grep，`agent-capability-declaration.md` §三 实测表的"2026-06-24"标注需更新——它是时间快照。
4. 本批次为纯新增文档，零删除、零改 .java/pom.xml/CLAUDE.md，回滚成本极低（`git checkout` 即还原）。
